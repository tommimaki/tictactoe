//gameboard object, module pattern
const gameBoard = (() =>{

    const grid = [1,2,3,
                   4,5,6,
                    7,8,9]
    return{
        //jotain
    }
})();



// player object

const Player = (move, score) => {
    
        const move = x =>{
            //set X / O
        }
    };

    const Controller = (score) => {
        const score = 0;
    }